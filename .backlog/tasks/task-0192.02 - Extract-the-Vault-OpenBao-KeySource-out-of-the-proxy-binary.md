---
id: TASK-0192.02
title: Extract the Vault/OpenBao KeySource out of the proxy binary
status: To Do
assignee: []
created_date: '2026-08-19 16:59'
updated_date: '2026-08-20 18:34'
labels:
  - library
  - refactor
dependencies:
  - TASK-0006
  - TASK-0024
  - TASK-0083
parent_task_id: TASK-0192
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
VaultKeySource lives in crates/proxy/src/vault.rs (~1,300 LOC), so the library ships only FileKeySource — whose own doc comment says dev/test. The README advertises Vault/OpenBao-backed keys as a headline feature; it is not a library feature today.

What has to travel: Transit-wrapped DEK minting and unwrap-on-demand, the KV v2 index-key store with cas = 0 minting and versioned records, the legacy shared-map migration, the DEK/index caches and the token-lease watchdog.

Shape: a separate dbsec-vault crate is preferred over a feature on dbsec-core, so that vaultrs, url and a TLS stack stay out of the dependency graph of an application that only wants FileKeySource or its own KeySource impl. Either way the constraint is that dbsec-core keeps no dependency on any one key backend — Error::KeyBackend already boxes the cause for exactly this reason.

The token watchdog is the one piece that needs an async runtime; keep it optional so a synchronous embedder is not forced to take tokio.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 The Vault key source is usable from a library-only application with no dbsec-proxy dependency
- [ ] #2 dbsec-core still has no vaultrs/url dependency
- [ ] #3 make e2e-vault passes unchanged
- [ ] #4 The token-lease watchdog does not force tokio on an embedder that does not want it
<!-- AC:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Decision to make in this task, not after: KeySource (crates/core/src/keys.rs) is a sync trait. In a library linked into a tokio application every blocking Vault round trip stalls the embedder's worker (TASK-0016 is the proxy-side symptom). Recommended: keep the trait sync and guarantee the Vault source touches the network only on cold miss and rotation, with the token watchdog as the only async part; document the guarantee. Alternative is an async trait — pick one here.

TASK-0006, TASK-0024 and TASK-0083 are HIGH key-handling bugs that would ship as public-crate bugs once the Vault source is extracted; they are dependencies of this task.
<!-- SECTION:NOTES:END -->
