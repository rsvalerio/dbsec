[package]
name = "dbsec-pgwire"
description = "Sans-io framing for the PostgreSQL v3 wire protocol"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true
readme = "README.md"
documentation = "https://docs.rs/dbsec-pgwire"
keywords = ["postgresql", "protocol", "wire", "sans-io", "database"]
categories = ["database", "network-programming", "parser-implementations"]

[dependencies]
thiserror = { workspace = true }

[lints]
workspace = true

[dev-dependencies]
proptest = { workspace = true }
