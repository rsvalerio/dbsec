[package]
name = "dbsec-derive"
description = "#[derive(Protect)] for dbsec-core: record-level field encryption over a Protector"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true
readme = "README.md"
documentation = "https://docs.rs/dbsec-derive"
keywords = ["encryption", "derive", "field-level"]
categories = ["cryptography", "database"]

[lib]
proc-macro = true

[dependencies]
syn = { workspace = true }
quote = { workspace = true }
proc-macro2 = { workspace = true }

[lints]
workspace = true
